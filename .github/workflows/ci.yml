name: Payment CI Pipeline
run-name: Payment CI Pipeline

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  lint:
    runs-on: self-hosted
    steps:
      - run: echo "pylint *.py"

  test:
    needs: lint
    runs-on: self-hosted
    steps:
      - run: echo "Test Cases"

  ci:
    needs: test
    uses: raghudevopsb89/github-actions/.github/workflows/ci.yml@main
    with:
      component_name: payment

